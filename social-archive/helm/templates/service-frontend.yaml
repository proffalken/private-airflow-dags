apiVersion: v1
kind: Service
metadata:
  name: {{ include "social-archive.fullname" . }}-frontend
  labels:
    {{- include "social-archive.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "social-archive.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
